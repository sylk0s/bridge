use bridge_app::{Config, Server};

fn main() {
    // read and parse config
    // TODO change config structure (when I implement it for the main webserver)
     
    // for each server, spawn a new thread sending the output to each of the listening servers
}
